package com.lucas.apploginfirebasecompose.ui.theme

import androidx.compose.ui.graphics.Color

val Primary = Color(0xFF90CAF9) // Azul claro (pode trocar)
val OnPrimary = Color(0xFF000000) // Preto (texto, ícones)
val Background = Color(0xFFFFFFFF) // Branco
val OnBackground = Color(0xFF000000) // Preto (texto em fundo branco)
val Surface = Color(0xFFFFFFFF) // Fundo dos cards, inputs
val OnSurface = Color(0xFF000000) // Texto nos inputs
val Error = Color(0xFFFF5252)
val OnError = Color(0xFFFFFFFF)
